---
description: Run an approved, bounded change-and-benchmark research loop
argument-hint: "<measurable optimization goal | off | clear>"
---
Run or control an autoresearch loop for: $@

Read `~/.pi/agent/skills/autoresearch/SKILL.md` and its referenced evidence guidance. Use the default Pi agent and existing tools. Require the benchmark, metric, change scope, iteration cap, environment, and explicit approval before running. `off` preserves state; `clear` requires confirmation before removing only this workflow's own state.
