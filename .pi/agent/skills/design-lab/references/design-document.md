# Design document

Resolve the accepted implemented surface. Read
`~/.agents/skills/impeccable/SKILL.md` and its `reference/document.md` and
follow the **current** Impeccable document workflow rather than copying an old
schema. Inspect the actual CSS, tokens, assets, responsive behavior, and
verification evidence. If root `DESIGN.md` exists, show it and ask whether to
refresh, merge, or overwrite before editing.

In scan mode, write `DESIGN.md` and the Impeccable sidecar from observed values
using the current canonical format; distinguish browser-local user reports from
verified implementation. Validate the document, JSON if generated, and
referenced paths. Report what was recorded and stop.
