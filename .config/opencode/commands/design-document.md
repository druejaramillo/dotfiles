---
description: Capture an accepted implemented visual system in DESIGN.md and its Impeccable sidecar.
agent: design-orchestrator
---

Run the design-document phase for `$ARGUMENTS`. Resolve the accepted surface or
prototype from the argument or active conversation, then load the Impeccable
document workflow. Inspect the implemented surface, its CSS or tokens, product
constraints, asset strategy, responsive behavior, components, and verification
evidence.

If `DESIGN.md` already exists, show the current file and ask whether to refresh,
merge, or overwrite before editing it. Otherwise write `DESIGN.md` and
`.impeccable/design.json` using the documented schema and canonical section
order. Capture confirmed decisions and actual tokens; label user-reported
browser-local settings as reported rather than verified implementation state.

Validate the JSON sidecar, the document structure, all referenced local paths,
user explicitly expands scope.
