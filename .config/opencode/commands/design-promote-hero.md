---
description: Integrate a chosen hero study into a preserved full-page prototype and complete the surrounding page.
agent: design-orchestrator
---

Run the hero-promotion phase for `$ARGUMENTS`. Require both a resolvable
full-page prototype and a selected hero study. Inspect the prototype, chosen
hero, approved content sources, and existing exploration lab before editing.

Integrate the selected hero as the full page's first viewport without removing
the hero lab, picker, alternative studies, or comparison routes. Preserve the
chosen composition as visual truth. Expand downstream content only with facts
approved by the product sources; strengthen hierarchy, add complete visitor
information, and maintain the selected page's visual grammar.

Add hero entrance, reveal, and hover feedback appropriate to the chosen art
source facts, detector findings, syntax, formatting, and the active project
build. Report the preserved exploration artifacts alongside the promoted page.

## First-render and transition contract

Treat the selected hero's settled composition as visual truth. After entrance
motion completes, capture desktop and mobile renders and reject clipped
ascenders, descenders, image crops, or hero overflow. Preserve a typography
safe area around transformed display text rather than relying on tight line
boxes or animation clipping.

Build a continuous, visibly populated hero-to-body bridge using the promoted
page's existing material grammar. The bridge and first downstream section must
be visible without JavaScript and under reduced motion. Do not use
IntersectionObserver, delayed animation state, `clip-path`, opacity, or any
other reveal effect to gate downstream content; motion may enhance already
visible content only.

Browser-test the actual served route at the hero/body boundary and within at
least one downstream section. Confirm non-empty rendered text, no blank gaps,
and responsive layout. Re-test invalid, success, and reset interest-form
states after promotion. Validate the Vite development route and production
preview alongside detector, syntax, formatting, and root build checks.
