---
description: Create a reviewable YAML outline and field schema for resumable multi-entity research.
agent: research-orchestrator
---

Set up a structured research catalog for `$ARGUMENTS`. Load `deep-research` and
create `research/<topic-slug>/outline.yaml` and `fields.yaml`. First propose a
decision-relevant item list and field framework, supplement it with one bounded
source-grounded research pass, then show the proposed schemas and require
confirmation before saving them. Set the default batch size to three and do not
start `/research-deep` in this command.
